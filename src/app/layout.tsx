import type { Metadata } from "next";
import localFont from "next/font/local";
import "./globals.css";

const geistSans = localFont({
  src: "./fonts/GeistVF.woff",
  variable: "--font-geist-sans",
  weight: "100 900",
});
const geistMono = localFont({
  src: "./fonts/GeistMonoVF.woff",
  variable: "--font-geist-mono",
  weight: "100 900",
});

export const metadata: Metadata = {
  title: "Lumovate Intelligence - Shaping Tomorrow with Intelligent Innovation",
  description:
    "Lumovate Intelligence is a forward-thinking technology company that develops innovative and futuristic products. We shape tomorrow by merging luminous ideas with intelligent innovation.",
  keywords:
    "Lumovate Intelligence, futuristic technology, innovation, intelligent solutions, luminous ideas, tech products, technology company, future innovation, AI, tech innovation, shaping tomorrow",
  viewport: "width=device-width, initial-scale=1.0",
  robots: "index, follow",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
